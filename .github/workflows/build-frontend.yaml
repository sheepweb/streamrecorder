name: Frontend - Build and Push Docker Image

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/frontend

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_PLATFORM=${{ secrets.NEXT_PUBLIC_PLATFORM }}
            NEXT_PUBLIC_BASE_URL=${{ secrets.NEXT_PUBLIC_BASE_URL }}
            NEXT_PUBLIC_FREEMIUS_PRODUCT_ID=${{ secrets.NEXT_PUBLIC_FREEMIUS_PRODUCT_ID }}
            NEXT_PUBLIC_FREEMIUS_PLAN_ID=${{ secrets.NEXT_PUBLIC_FREEMIUS_PLAN_ID }}
            NEXT_PUBLIC_FREEMIUS_PUBLIC_KEY=${{ secrets.NEXT_PUBLIC_FREEMIUS_PUBLIC_KEY }}
            NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
            STRAPI_URL=${{ secrets.STRAPI_URL }}
            GLITCHTIP_AUTH_TOKEN=${{ secrets.GLITCHTIP_AUTH_TOKEN }}
            GLITCHTIP_ORG=${{ secrets.GLITCHTIP_ORG }}
            GLITCHTIP_PROJECT=${{ secrets.GLITCHTIP_PROJECT }}
            GITHUB_SHA=${{ github.sha }}

      - name: Trigger Coolify Deploy
        run: |
          curl -X GET "${{ secrets.COOLIFY_FRONTEND_WEBHOOK }}" \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}"
